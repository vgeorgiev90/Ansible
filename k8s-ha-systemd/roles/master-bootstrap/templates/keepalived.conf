! Configuration File for keepalived

global_defs {
   notification_email {
     viktor.georgiev@example.com
   }
   notification_email_from lb1@kubernetes.cluster
   smtp_server localhost
   smtp_connect_timeout 30
}

vrrp_instance kubernetes {
    state MASTER
    interface {{ keepalived_interface }}
    virtual_router_id 101
    priority 101
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass gjGDHR34d
    }
    virtual_ipaddress {
         {{ kube_addr }}
    }
}
